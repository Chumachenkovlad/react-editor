export const DEFAULT_CONTENT = `Here is a text editor component consist of <span class="editable" style="font-style: italic;"><span class="editable" style="font-size: 20px; text-decoration: underline; font-style: italic;">header</span></span> and <span class="editable" style="font-style: italic;"><span class="editable" style="font-size: 20px; font-style: italic; text-decoration: underline;">textarea</span></span>. The header <span class="editable" style="text-decoration: underline; font-style: italic;">consist of</span> a set of controls. Some of them are just togglers when others could open <span class="editable"><span class="editable" style="">dialog</span></span> to specify complex behavior. Controls could be configured at <span class="editable" style="color: rgb(74, 144, 226); background: rgb(248, 231, 28); font-style: italic; font-weight: bold; text-decoration: underline;">formattings.ts</span> where declared predefined list of Formattings. The main idea is to wrap selected text with an element and then save each formatting as an element attribute value or part of style attribute. 
For complex behavior, <span class="editable" style="font-weight: bold;">EditorWidgetFactory</span> could be extended with other Widget that accepts the previous value and returns a new one.
Word could be selected by double click.
Also, range selection could be used.`;
